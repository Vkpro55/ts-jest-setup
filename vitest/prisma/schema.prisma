generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model Sum {
  id          Int @id   @default(autoincrement())
  a           Int
  b           Int
  result      Int
}